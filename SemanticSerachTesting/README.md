# PubMed Semantic Search (FastAPI backend + Streamlit frontend)

## Setup (from scratch)

1. Create venv and install:
```bash
python -m venv .venv
# activate venv
# Windows
.venv\Scripts\activate
# mac/linux
source .venv/bin/activate

pip install -r requirements.txt
